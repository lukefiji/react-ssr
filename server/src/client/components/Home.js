import React from 'react';

const Home = () => {
  return (
    <div>
      I'm the new home component
      <button onClick={() => console.log('Clicked!')}>Click</button>
    </div>
  );
};

export default Home;
